{
  "name": "weather-journal-app",
  "version": "1.0.0",
  "description": "## server.js run local server using node_modules install express , body-parser and cors and used on the server listen to server 3013 add get and post methods to get data from app to api server",
  "main": "server.js",
  "dependencies": {
    "body-parser": "^1.19.0",
    "cors": "^2.8.5",
    "express": "^4.17.1"
  },
  "devDependencies": {},
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "author": "ahmed mostafa",
  "license": "ISC"
}
